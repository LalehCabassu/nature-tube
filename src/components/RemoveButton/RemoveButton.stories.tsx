/* eslint-disable */
import RemoveButton from './RemoveButton';

export default {
    title: "RemoveButton",
};

export const Default = () => <RemoveButton onClick={() => {
}}/>;

Default.story = {
    name: 'default',
};
